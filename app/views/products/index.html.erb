<h1>Shopping Cart</h1>

<%= form_tag(products_add_path) do %>
  <%= text_field_tag :product %>
  <%= submit_tag 'add to cart' %>
<% end %>

Cart:<ul>
  <% cart.each do |item| %>
    <li><%= item %></li>
  <% end %>
</ul>
